apiVersion: v1
kind: Service
metadata:
  name: node-app-service
spec:
  type: NodePort
  selector:
    app: node-app
  ports:
    - port: 3000          # Service içindeki port
      targetPort: 3000   # Container port
      nodePort: 31000    # Dış dünyadan erişim
